# Use a slim python image to keep the memory dump size manageable
FROM python:3.11-slim

# Set working directory
WORKDIR /app

COPY app.py /app/server.py
# Expose the port
EXPOSE 8080

# Run unbuffered so logs appear instantly
CMD ["python", "-u", "server.py"]
